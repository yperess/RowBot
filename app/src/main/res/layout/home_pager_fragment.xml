<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:custom="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.astuetz.PagerSlidingTabStrip
        android:id="@+id/tabs"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_alignParentTop="true"
        android:background="#33b5e5"
        android:textColor="@android:color/white"
        custom:pstsShouldExpand="true"
        custom:pstsIndicatorColor="@android:color/white"
        custom:pstsDividerColor="@android:color/transparent"
        custom:pstsUnderlineColor="@android:color/transparent"
        custom:pstsIndicatorHeight="4dp"
        custom:pstsDividerPadding="0dp"/>

    <android.support.v4.view.ViewPager
        android:id="@+id/pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/tabs"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="@dimen/title_shadow_height"
        android:background="@drawable/toolbar_shadow"
        android:layout_below="@+id/tabs"/>

</RelativeLayout>
